import 'package:flutter/material.dart';

class FavouriteHome extends StatelessWidget {
  const FavouriteHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Favourite Home"),
      ),
    );
  }
}
